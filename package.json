{
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf bin",
    "start": "parcel index.html --out-dir bin --open",
    "build": "parcel build index.html"
  },
  "dependencies": {
    "pixi.js": "^4.8.2"
  },
  "devDependencies": {
    "parcel-bundler": "1.10.0",
    "parcel-plugin-json-url-loader": "^0.1.1",
    "parcel-plugin-static-files-copy": "^1.2.1"
  }
}
